import bpy

bpy.context #erlaubt Zugriff auf den akt. Kontext, in dem sich der Benutzer befindet, z.B. Szene, Modus,...

bpy.data # die .blend- Datei wird damit abgebildet

bpy.ops # Operatoren = Transform, Position im Raum usw.

bpy.types # Blenders interne Datentypen, z.B. Objekte, Materialien,...